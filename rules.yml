groups:
  - name: default
    rules:
      - alert: ServerDown
        expr: up == 0
        for: 1m
        labels:
          severity: high
        annotations:
          summary: 'xaidaz.com är nere!'